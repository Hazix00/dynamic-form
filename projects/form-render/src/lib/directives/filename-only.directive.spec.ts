import { FilenameOnlyDirective } from './filename-only.directive';

describe('FilenameOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new FilenameOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
